@model IEnumerable<Category>
@{
    ViewData["Title"] = "Danh mục";
}

@section PageHeaderRight {
    <a asp-action="Create" class="btn btn-primary">
        <i class="feather-plus me-2"></i>
        <span>Thêm danh mục</span>
    </a>
}

<div class="row">
    <div class="col-lg-12">
        <div class="card stretch stretch-full">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th class="wd-30">
                                    <div class="custom-control custom-checkbox ms-1">
                                        <input type="checkbox" class="custom-control-input" id="checkAll">
                                        <label class="custom-control-label" for="checkAll"></label>
                                    </div>
                                </th>
                                <th>STT</th>
                                <th>Tên</th>
                                <th>Slug</th>
                                <th>Danh mục cha</th>
                                <th>Thứ tự</th>
                                <th>Trạng thái</th>
                                <th class="text-end">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Any())
                            {
                                @foreach (var c in Model)
                                {
                                    <tr class="single-item">
                                        <td>
                                            <div class="custom-control custom-checkbox ms-1">
                                                <input type="checkbox" class="custom-control-input checkbox" id="checkBox_@c.Id">
                                                <label class="custom-control-label" for="checkBox_@c.Id"></label>
                                            </div>
                                        </td>
                                        <td>
                                            <span class="badge bg-soft-primary text-primary">@c.Id</span>
                                        </td>
                                        <td>
                                            <span class="fw-semibold d-block" style="max-width: 150px;">@c.Name</span>
                                        </td>
                                        <td>
                                            <span class="text-muted fs-12 text-truncate d-block" style="max-width: 150px;">@c.Slug</span>
                                        </td>
                                        <td>
                                            <span class="badge bg-soft-info text-info">@(c.Parent?.Name ?? "—")</span>
                                        </td>
                                        <td>@c.DisplayOrder</td>
                                        <td>
                                            @if (c.IsActive)
                                            {
                                                <span class="badge bg-soft-success text-success">Hiển thị</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-soft-secondary text-secondary">Ẩn</span>
                                            }
                                        </td>
                                        <td>
                                            <div class="hstack gap-2 justify-content-end">
                                                <a asp-action="Edit" asp-route-id="@c.Id" class="avatar-text avatar-md" data-bs-toggle="tooltip" title="Sửa">
                                                    <i class="feather-edit-3"></i>
                                                </a>
                                                <div class="dropdown">
                                                    <a href="javascript:void(0)" class="avatar-text avatar-md" data-bs-toggle="dropdown" data-bs-offset="0,21">
                                                        <i class="feather-more-horizontal"></i>
                                                    </a>
                                                    <ul class="dropdown-menu dropdown-menu-end">
                                                        <li>
                                                            <a class="dropdown-item" asp-action="Edit" asp-route-id="@c.Id">
                                                                <i class="feather-edit-3 me-3"></i>
                                                                <span>Sửa</span>
                                                            </a>
                                                        </li>
                                                        <li class="dropdown-divider"></li>
                                                        <li>
                                                            <form asp-action="Delete" asp-route-id="@c.Id" method="post" class="m-0">
                                                                @Html.AntiForgeryToken()
                                                                <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Xóa danh mục này?');">
                                                                    <i class="feather-trash-2 me-3"></i>
                                                                    <span>Xóa</span>
                                                                </button>
                                                            </form>
                                                        </li>
                                                    </ul>
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="8" class="text-center py-5">
                                        <p class="text-muted mb-0">Chưa có danh mục nào.</p>
                                        <a asp-action="Create" class="btn btn-sm btn-primary mt-2">
                                            <i class="feather-plus me-2"></i>Thêm danh mục đầu tiên
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
